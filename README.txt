USAGE:
	Requires:
        QMake version 3.1
        Using Qt version 5.12.8
        * Can use "sudo apt-get install qt5-default" on Ubuntu to get this version *

	Compile the program using:
		qmake WeatherApp.pro && make
		
	Run the program using:
		./WeatherApp